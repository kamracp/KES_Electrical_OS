// Placeholder so the route guard has a destination; the sign-in form is EOS-01 (a) commit 16.
// Nothing is released before commit 21, so this text never reaches the live site.
export function LoginPage() {
  return (
    <main aria-labelledby="login-title">
      <h1 id="login-title">Sign in</h1>
      <p>KES Electrical OS is available to signed-in users only.</p>
    </main>
  );
}
