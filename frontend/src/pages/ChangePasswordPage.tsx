// Placeholder so the route guard has a destination; the form arrives with the sign-in pages
// of EOS-01 (a). Nothing is released before commit 21.
export function ChangePasswordPage() {
  return (
    <main aria-labelledby="change-password-title">
      <h1 id="change-password-title">Change your password</h1>
      <p>A first or reset password must be changed before any study can be opened.</p>
    </main>
  );
}
